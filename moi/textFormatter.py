class TextFormatter:
    """
    The tokenize and keyword libraries.
    """


    DEFAULT_FORMAT = None

    def __init__(self):
        pass

    @staticmethod 
    def merge(old_format, new_format):
        """
        The ability to keep certain format properties unchanged.

        To handle the case where a link belongs to a selection
        containing other tags that are not links.
        """
        pass

    @staticmethod 
    def compare(format_one, format_two):
        pass
